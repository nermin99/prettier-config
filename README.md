# `@nermin99/prettier-config`

> My personal [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
npm install --save-dev @nermin99/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@nermin99/prettier-config"
}
```
